Wire frame directory will contain directories with '<ACTOR_NAME> - <DIALOGUE_NAME>' format

Each wire frame directory will contain 
   - png images showing the expected screen layout.
   - a pencil design file (can be opend with pencil tool to modify)
